# Bingo Framework Evolution

## 定位

Bingo 当前定位是脚手架模板（`bingo create` 生成源码副本），计划演进为可导入的框架。

### 当前状态

- 包名：`bingo`（简单名称，无法被外部 `go get`）
- 模式：源码复制 + 包名替换
- 问题：`pkg/ws` 等通用能力无法被外部项目直接导入

### 目标状态

- 包名：`github.com/bingo-project/bingo`
- 模式：框架被 import，用户专注业务逻辑
- 参考：借鉴 controller-runtime (K8s) 和 Kratos 的架构设计

## 框架定位与优势

### 一句话定位

> **Bingo: 开箱即用的 Go 应用框架，专注业务开发，渐进式扩展**

### 市场空白

Go 生态缺少一个 "中间地带" 的框架：

```
Gin/Echo                      Bingo                     Kratos/Go-Zero
   │                            │                            │
   ▼                            ▼                            ▼
轻量路由框架               渐进式全栈框架               微服务框架
自己搭积木                 开箱即用+可扩展              全家桶+强约束
适合小项目                 适合中小型应用               适合大型分布式
```

### 核心优势

| 优势 | 说明 | vs 竞品 |
|------|------|---------|
| **业务基座开箱即用** | apiserver + adminserver(RBAC) + scheduler | Gin 没有，Kratos 需要自己搭 |
| **完整工具链** | create, make, migration, seeder | 不亚于 artisan，比 Gin 生态完整 |
| **多协议统一** | HTTP/gRPC/WebSocket 一套配置 | Gin 只有 HTTP |
| **渐进式复杂度** | 小项目直接用，大项目按需集成 | Kratos 一上来就很重 |
| **不强绑定** | 不需要 protobuf/特定 DSL | Go-Zero 强依赖 .api 文件 |

### 核心价值

1. **Day 1 生产力** - 新项目不需要花时间搭积木，直接写业务
2. **多协议统一** - HTTP/gRPC/WebSocket 一套配置、一套中间件抽象
3. **工具链辅助** - bingoctl 提升开发效率，但不强绑定
4. **渐进式扩展** - 小项目简单用，大项目可逐步引入服务发现、熔断等能力

### 扩展能力策略

对于服务发现、熔断限流等高级能力：**提供集成能力，而非自研**

理由：
- 这些是基础设施层，有成熟方案（Consul/Nacos/Sentinel）
- 用户可能已有技术栈，强制替换成本太高
- 通过 Runnable 方式集成，保持框架轻量

```go
// 示例：按需集成高级能力
bingo.New().
    Add(
        discovery.NewConsul(),
        circuitbreaker.NewSentinel(),
    ).
    Run(ctx)
```

## 演进进展

### 当前阶段

**Phase 0: 依赖分析与解耦** - ✅ 完成

### 进展概览

| Phase | 名称 | 状态 | 说明 |
|-------|------|------|------|
| 0 | 依赖分析与解耦 | ✅ 完成 | pkg/* 不再依赖 internal/* |
| 1 | 包名变更 | 🔲 未开始 | `bingo` → `github.com/bingo-project/bingo` |
| 2 | 接口抽象重构 | 🔲 未开始 | App 层、Runnable、核心接口 |
| 3 | 分离演示项目 | 🔲 未开始 | 创建 starter 仓库 |

## 迁移计划

### Phase 0: 依赖分析与解耦

- [x] 生成 internal/pkg 的完整依赖图
- [x] 识别循环依赖和跨层依赖
- [x] 在当前包名下解决依赖问题
- [x] 确保 pkg/* 不依赖 internal/*

#### 依赖分析结果

**违规依赖（pkg → internal）**

| pkg 包 | 违规依赖 | 修复方案 |
|--------|----------|----------|
| `pkg/db` | `internal/pkg/logger` | 接受 `gorm.Logger` 参数注入 |
| `pkg/ws` | `internal/pkg/log` | 默认 `nopLogger`，可选注入 |

**跨层依赖（internal/pkg → 业务）**

| 包 | 违规依赖 | 修复方案 |
|----|----------|----------|
| `bootstrap` | `internal/scheduler/biz/syscfg` | Phase 2 重构时解决 |

#### 包边界决策

**移到 pkg/（框架公开 API）**

| 包 | 说明 |
|----|------|
| `log` | 日志接口，核心能力 |
| `config` | 配置结构，用户需要 |
| `server` | Server 抽象，核心能力 |
| `auth` | 认证能力，通用 |
| `core` | Response 等核心类型 |

**保留 internal/（业务相关）**

| 包 | 说明 |
|----|------|
| `errno` | 业务错误码 |
| `bootstrap` | 启动逻辑，Phase 2 融入 App 层 |
| `store` | 业务 Store 实现 |
| `model` | 业务模型 |
| `facade` | 业务门面 |
| `known` | 业务常量 |
| `task` | asynq 任务相关 |
| `middleware` | 需拆分（通用部分公开） |
| `database` | migration/seeder，演示代码 |
| `eth` | 业务相关 |
| `logger` | GORM 日志适配器，随 log 一起处理 |
| `util` | 内部工具 |

### Phase 1: 包名变更

- [ ] 将 `go.mod` 中的 `module bingo` 改为 `module github.com/bingo-project/bingo`
- [ ] 全局替换所有 import 路径
- [ ] 更新文档和示例

### Phase 2: 接口抽象重构

- [ ] 实现 App 层作为统一入口
- [ ] 实现 Runnable 接口和内置 Runnable
- [ ] 定义核心接口：Logger, Config, Server 等
- [ ] 将 `internal/pkg/server` 移动到 `pkg/server`
- [ ] 重构依赖，通过接口注入而非直接导入

### Phase 3: 分离演示项目

- [ ] 创建 `github.com/bingo-project/starter` 仓库
- [ ] 迁移业务代码：cmd/, internal/apiserver/, internal/admserver/ 等
- [ ] 演示项目 import 框架

## 后续工作

以下工作在核心框架稳定后再考虑：

### bingoctl 适配

- [ ] 修改 `bingo create` 从 starter 仓库 clone
- [ ] 替换包名逻辑

## 决策记录

### 基础决策

| 主题 | 决策 | 理由 |
|------|------|------|
| 框架名称 | bingo | 保持现状 |
| Organization | bingo-project | 保持现状 |
| 脚手架项目 | starter | Kratos 风格（kratos + kratos-layout） |
| component-base | 废弃 | cli → bingoctl，web 组件 → bingo/pkg |
| 版本管理 | 暂不用 `/v2` | 当前是 v0 阶段 |
| 向后兼容 | 不提供迁移路径 | 老项目继续老方式 |
| pkg 边界 | Phase 0 确定 | 依赖分析时确定 |
| 文件夹命名 | 统一单数 | 符合 Go 惯例（router/, handler/） |

### 架构决策

| 主题 | 决策 | 理由 |
|------|------|------|
| 核心抽象 | Runnable 和 Registrar 两个独立接口 | 职责分离，避免空实现 |
| Runnable 接口 | 只有 Start(ctx)，Name() 作为可选接口 | 保持最简，Named 接口用于日志 |
| 添加组件 | Add(Runnable) 和 Register(Registrar) | 类型安全，API 意图明确 |
| 多协议 Server | 配置驱动 + 并发启动 | HTTP/gRPC/WS 无依赖，可并发 |
| 依赖共享 | 统一方法访问，不暴露字段 | API 一致，可加逻辑，封装性好 |
| 可选组件访问 | 未配置时 panic | Fail-fast，stack trace 足够定位 |
| New() 职责 | 解析配置，返回 error | 可测试，库不应直接 os.Exit |
| Init() 职责 | 初始化基础依赖（DB/Cache 等） | Server 在 Run() 时创建，CLI 场景轻量 |
| CLI 支持 | Init() + Close()，不强制 Run() | CLI 工具也能复用依赖管理 |
| 配置文件 | WithConfigName 指定，默认 "config" | 多服务项目需显式指定 |
| 配置搜索路径 | -c 命令行 > 环境变量 > ./config.yaml > 标准路径 | 灵活且符合惯例 |
| 配置热更新 | 不支持 | 依赖 K8s 滚动更新，简化复杂度 |
| 中间件 | 核心逻辑复用，包装层各协议独立 | 避免抽象泄漏，HTTP/gRPC/WS 签名本质不同 |
| 链路追踪 | 内置 Request ID + Client IP，OpenTelemetry 可选 | 轻量开箱即用，复杂场景按需扩展 |
| Metrics | 可选 Runnable，用户按需添加 | 不强制，用户可能已有方案 |
| Panic 处理 | 请求级 recover，Runnable 级让进程崩 | Fail fast，K8s 会重启 |
| Testing | 不内置 mock，提供 NewTestApp + Ready() channel | 灵活，Ready() 用于等待就绪 |
| 依赖注入 | 可选集成 Wire，框架不强绑定 | Wire 对大项目有价值，小项目手动够用 |
| 任务队列 | 继续使用 asynq | 功能丰富（队列/定时/延迟/重试），已有积累 |
| Leader Election | 不内置 | asynq 已解决定时任务/队列场景，Bingo 非 Operator 框架 |
| 包组织 | 根包 re-export 常用类型 | 类似 controller-runtime，用户只需 import 一个包 |
| 框架边界 | 提供通用实现（auth/middleware 等） | 开箱即用，用户可通过接口覆盖 |
| 并发安全 | Add/Register 不支持并发 | 实际场景不需要，YAGNI |
| WebSocket 归属 | 独立仓库 bingo-project/websocket | 用户按需包装成 Runnable |
| Register 约定 | 不应有外部副作用 | 只做内存注册，外部操作在 Start 或脚本中 |

### 生命周期决策

| 主题 | 决策 | 理由 |
|------|------|------|
| 生命周期控制 | context 驱动 | ctx.Done() 触发关闭，Go/K8s 生态惯例 |
| 启动顺序 | Register 串行 → Start 并发 | Register 有依赖，Server 间无依赖 |
| Register 执行顺序 | 按添加顺序 | 直观可预测，用户代码顺序即执行顺序 |
| grpc-gateway 依赖 | 端口探测 | Gateway 启动时等待 gRPC 端口可连接，简单可靠 |
| 就绪通知 | app.Ready() channel | 框架 markReady 时 close channel，测试可等待 |
| 信号处理 | 用户控制 | 提供 SetupSignalHandler()，类似 controller-runtime |
| 关闭触发 | context 取消 | 统一机制，无需显式 Shutdown 方法 |
| Shutdown 超时 | App 层统一兜底 | Runnable 不设超时，App 层做最终超时控制 |
| 关闭失败处理 | 继续关闭其他，返回首个错误 | best-effort，业界标准 |

### 健康检查决策

| 主题 | 决策 | 理由 |
|------|------|------|
| 端点暴露 | 独立 HTTP 端口 | 与业务 Server 解耦，始终可用 |
| 端点路径 | /healthz, /readyz | K8s 生态惯例 |
| 检查机制 | 不提供自定义，固定行为 | 自定义检查大多是错误做法 |
| Liveness | 始终 200 | 能响应即存活，不检查外部依赖 |
| Readiness | 框架状态控制 | 启动完成 200，关闭时 503 |
| Server 故障 | 进程退出 | Runnable 失败触发进程崩溃，K8s 检测后重启 |

## 文档索引

- [整体架构设计](./architecture.md)
- 核心概念
  - [lifecycle.md](./concepts/lifecycle.md) - 生命周期管理
  - [runnable.md](./concepts/runnable.md) - Runnable 设计
  - [app.md](./concepts/app.md) - App 层设计
  - [health.md](./concepts/health.md) - 健康检查设计

## 参考

### 主要参考（Go 生态）

- [controller-runtime](https://github.com/kubernetes-sigs/controller-runtime) - K8s 控制器框架，Manager 模式、依赖管理
- [Kratos](https://github.com/go-kratos/kratos) - 微服务框架，包名 `github.com/go-kratos/kratos/v2`
- [Go-Zero](https://github.com/zeromicro/go-zero) - 微服务框架

### 设计原则

1. **首选 Go 生态惯例**：controller-runtime 的 Manager 模式
2. **类型化优于通用容器**：`app.DB()` 优于 `app.Get("db")`
3. **编译期优于运行时**：不做运行时反射注入
